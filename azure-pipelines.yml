# specific branch build Trigger
trigger:
  branches:
    include:
      - main
      - azure-pipelines
    exclude:
      - releases/old*

# Tasks
jobs:
  - job: Windows
    pool:
      name: az400mlab-pool1
      demands:
        - agent.name -equals MMD5CG20976RX
    steps:
      - script: echo hello from Windows

      - task: TerraformTaskV1@0
        inputs:
          provider: "azurerm"
          command: "init"
          workingDirectory: "C:\Terraform"
          environmentServiceName: "Azure-SP-connect"
          #end1